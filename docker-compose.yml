version: "3.9"

services:

  mlflow:
    build: mlflow
    ports:
      - "5000:5000"
    volumes:
      - ./mlruns:/mlruns

  training:
    build: services/training
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data
      - ./models:/models
    depends_on:
      - mlflow
